<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title(id='main-title')= title
    body
        h1(class=["main-heading"]) My App in PUG
        ul
            each val in bookList
                li= val
